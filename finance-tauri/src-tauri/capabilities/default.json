{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The frontend talks to the embedded HTTP backend directly (axios), so no extra Tauri command/plugin permissions are required.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
