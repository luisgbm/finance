{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The frontend talks to the embedded HTTP backend directly (axios); it also forwards console output to the log plugin.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default"]
}
